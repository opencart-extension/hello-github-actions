name: A workflow for my Hello World file
on: push

jobs:
    build:
        name: Hello world action
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v1
              
            - name: Action Dockerfile
              uses: ./action-a
              with:
                  MY_NAME: "Valdeir Psr"

            - name: Check file
              run: echo $INPUT_MY_NAME
